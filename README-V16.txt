V16: Home hero left untouched. Progress section pulled upward. Category cards now use clean high-resolution photographs with no baked-in text/icons. Activity cards 11-300 use the existing title-specific IMAGE_OVERRIDES through a rewritten same-origin image endpoint that filters out statues/cats/logos/illustrations and low-resolution results. No category-image fallback is used for activity cards.