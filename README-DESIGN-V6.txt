DESIGN V6
- Home rebuilt to mirror the approved mockup proportions and spacing.
- Hero image changed to a Positano/Amalfi Coast sunset image.
- All six Home progress cards remain image-backed.
- My List cards never use LoremFlickr.
- Every master goal keeps its own specific image search phrase (1–300).
- Card photos lazily resolve against the exact goal-specific phrase and are cached on-device.
- If a photo lookup fails, the card falls back to a tasteful category photo rather than an unrelated image.
- Supabase/auth/data logic is unchanged.
