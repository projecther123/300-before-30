V11 implements the locked Home layout: title left, Italy image right, progress circle below title, quote beside circle, no overlap.
My List cards use direct photographic URLs so images do not depend on a serverless API/CORS.
Upload index.html, styles.css and app.js to replace the existing files.
