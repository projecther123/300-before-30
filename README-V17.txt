V17 — IMAGE DELIVERY FIX ONLY
- Homepage untouched/locked.
- Existing successful goal photos #1–5 and #7–9 untouched.
- #6 and #10 now use the same title-specific image resolver as the remaining goals.
- #11–300 resolve from each goal's own curated title/query.
- /api/image now PROXIES image bytes instead of redirecting to third-party hosts, fixing the blank cards caused by hotlink/CORS/redirect failures on mobile Safari.
- No list titles, numbering, categories, progress logic, layout or homepage styling changed.
